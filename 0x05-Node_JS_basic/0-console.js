/**
 * displayMessage function displays a message to the console.
 *
 * @param {string} str - The message to be displayed.
 */
const displayMessage = (str) => console.log(str);

module.exports = displayMessage;
